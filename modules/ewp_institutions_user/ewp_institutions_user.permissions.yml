set any user institution:
  title: "Set any user's Institution"
  description: 'Should be combined with Administer users.'
  restrict access: true

set own user institution:
  title: 'Set own Institution'

select any institution:
  title: 'Select any Institution in filtered entity reference fields'

set own user institution via event:
  title: 'Set own Institution via SetUserInstitutionEvent'
  restrict access: true
